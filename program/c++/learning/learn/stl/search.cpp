//
// Created by Administrator on 2026/7/19.
//

#include <algorithm>
#include <iostream>
#include <vector>

// int main() {
//     std::vector<int> list;
//     for (int i = 0; i < 10; i++)
//         list.push_back(i);
//     list.push_back(9);
//     //二分查找
//     std::cout << std::ranges::binary_search(list, 5) << std::endl;
//
//     //查找容器内第一次出现的重复相邻数据，并返回它的起始index迭代器
//     const auto &it = _STD ranges::adjacent_find(list);
//     if (it != list.end())
//         std::cout << *it << std::endl;
//     return 0;
// }
