#引入全部匹配规则%。
#使用%来匹配所有的文件，如：
# %.c匹配所有的.c文件
# %.o匹配所有的.o文件
#语句 %.o : %.c表示任意名称的.o文件都依赖它自身名称的.c文件
#这样可不用逐条写对应关系，一条语句搞定
OBJ = test.o add.o sub.o mul.o div.o
TARGET = test
GCC = gcc
$(TARGET) : $(OBJ)
	$(GCC) $^ -o $@

%.o : %.c
	$(GCC) -c $^ -o $@
